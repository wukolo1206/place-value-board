# PITFALLS.md — 互動定位板

## 已知風險
（目前無未修項目）

---

## 「一十萬」vs「十萬」：不要憑語感假設

**現象**：`readNumber(100000)` 產出「一十萬」，但課本 p2 明寫「讀作**十萬**」。

**原因**：實作時我在註解裡寫「本教材四年級一律讀『一十』較清楚」——**那是假設，沒有查證**。
翻課本才發現 p2 寫「10 個 10000 合起來是 100000，讀作『十萬』」。

**解法**：規則是「一十」只在**整個數的開頭**讀成「十」，非開頭要保留。
證據：課本 p13 連連看有「八千零一十萬零六」（80100006），「一十」在中間就保留。
實作為讀完後檢查：若 token[0]==='一' 且 token[1]==='十'，去掉開頭的「一」。

**未來避免**：中文數字讀法牽涉大量約定俗成，**一律以課本實際出現的讀法為準**，
不要用語感推斷。課本標了讀法的數字都要納入回歸測試。

---

## 分出勝負後繼續點，會把正確結論覆蓋成錯的

**現象**：`compare.html` 比較 9490995 與 9629053，多點幾次「逐位比比看」後，
畫面顯示「每一位都一樣 → 9490995 ＝ 9629053」——**完全錯誤的結論**。

**原因**：分出勝負時我把 `step` 設成 `w` 想讓它停住，但下次點擊仍會 `step++`，
變成 `w+1` 就落入 `step >= w` 的「全部相等」分支。

**解法**：加獨立的 `decided` 旗標，一旦分出勝負就直接 `return`，不再往下比。

**未來避免**：教學工具的「結論」一旦顯示就不該被後續操作改寫。
用旗標明確表達狀態，不要用計數器的邊界值兼做狀態機。

---

## Codex 交叉審查抓到的問題（2026-08-23，22 條全數採納）

自己的 Playwright 測試 27 項全過，卻沒抓到下列任何一項 ——
**測試通過只代表沒測到問題，不代表沒問題**。

### 教學內容錯誤（會直接教錯，最嚴重）

**具體物層級錯了一層**
`board.html` 第一版直接從「1 萬元」開始，還把「一疊」說成 10 張 1 萬。
課本 p2 白紙黑字：「她把 10 張 **1000 元** 紮成一疊」「10 張 1000 元，合起來是 **10000** 元」。
**具體物是 1000 元鈔票，一疊＝10000 元。** 跳掉第一層化聚等於橋的左半邊沒搭。
修法：補回 1000 元券，且「紮成一疊」只在千→萬那一次講，萬→十萬改說「換成 1 個十萬」。

**顏色標示強化了錯誤策略**
`compare.html` 比 9000009 與 10000001 時，逐位標色把短數字的 9 標成綠（勝）、
長數字的 0 標成紅（敗），文字卻說「位數多的比較大」——**顏色與文字矛盾，且顏色那邊是錯的**。
修法：位數不同時**不進入逐位比較流程**，改整列標示。

**解析算理寫錯**
- 114 年第 5 題：右四位組是 `0700` 不是 `700`，我寫「夠減」是錯的
- 109 年第 5 題：概數題**必須先各自取概數再相減**（142754−122095＝20659萬）。
  先精確相減再換算會得到 20658 萬，與選項不符

**自組題與方法互相矛盾**
`123992+66889` 是我自組的卻標成「素養吧 p12」，右組 3992+6889=10881 是五位數，
與「右組四位、左組幾個萬」的分組法矛盾。已換成課本數字。

**改寫題目要消歧義**
提款單的欄名「仟」「拾」各出現兩次，原本沒指明左右。
另注意 `chStem` 用 `textContent`，題幹**不能用 HTML 標籤**。

### 程式缺陷

| 現象 | 根因 | 修法 |
|---|---|---|
| `99999999+1 → 90000000` 靜默倒退 | 進位到左界後把最高位夾回 9 | 先試算，放不下就自動展開；到真上限則保留原值並提示 |
| `123456789 → "12345 6789"` | 只切最後四位，沒有反覆分組 | 由右而左每四位一組 |
| `readNumber(1e12) → "一億"` | 沒有範圍契約，靜默給錯字串 | 明確契約 0～999999999，超出回傳說明 |
| 拖曳落點被忽略 | `target` 算出來只用於判空，之後永遠 `addAt(BILLS[k].idx)` | 真的認落點，放錯位拒絕並說位名 |
| 進位動畫看不到 | 對 `#stack` 節點加 class，但 `update()→drawStack()` 立刻重建 DOM | 改畫在 overlay 上 |
| 空白連按下一題就過關 | 關卡推進沒綁答對狀態 | `solved{stage_qi}`，整關答對才完成；轉場計時器捕捉固定目標 |
| 題幹問五個位只給三個輸入格 | `ask` 陣列與題幹文字不一致 | 補齊 |
| 減法可產生負數 | `canSimp()` 只檢查是否為萬的倍數 | 加 `A>=B`；`setEx()` 自動對調 |
| 觸控目標 32px | 只看視覺留白，沒量實際 CSS box | 五頁統一 `min-height:48px` |

### 通則

- **比對條件太寬 = 看起來有結果其實是假的**：
  `all([])` 為真、索引缺年級、拿 `專案總表.md` 比對單元名稱（列了全部單元，人人 match）
- **結論一旦顯示就不該被後續操作改寫**：用旗標表達狀態，不要用計數器邊界值兼做狀態機
- **狀態 A 成立才合法的功能，狀態 B 改變後要重新檢查**
