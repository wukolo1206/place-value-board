# 工作交接 — 2026-08-23

## 已完成

四上 U1「一億以內的數」互動定位板 **v1.2**，五頁 ＋ 可重跑測試。

### 經過兩輪 Codex 交叉審查
| 輪次 | 提出 | 採納 | 駁回 |
|---|---|---|---|
| 第一輪 | 22 | 22 | 0 |
| 第二輪 | 16 | 15 全採納、1 部分採納 | 0 |

第二輪判定「未通過」，其中**數條是我第一輪修正時自己製造的**：
- `setRange(7)` 當機（介面只有 6/8/9）
- 分組算理只移除觸發題目、算法沒修
- 文件改了核心章節卻漏掉其他兩處與 spec

### 主要修正
- **具體物層級**：補回 1000 元券（課本 p2 起點是「10 張 1000 元紮成一疊 ＝ 10000 元」）
- **顏色不再教錯**：位數不同時整列標示，不逐位比
- **分組算理**：低組正規化為 0~9999，進位借位反映到高組
- **溢位**：自動展開映射到合法 preset，到上限保留原值
- **轉場競態**：generation token，切關/切分頁使在途回呼失效
- **證據分級**：38 筆迷思概念回填結構化 `evidence_level`，閘門只讀該欄位且**真正阻斷**
- **術語誠實化**：`showScaffold()` 三特徵全未達成，改稱「表徵重呈」；
  「變異理論」改稱「候選關鍵特徵」；並更正我編造的課本 ⑨⑩⑪ 敘述

### 可重跑測試（新增）
```bash
python run-tests.py
```
核心邏輯抽到 `place-value-core.js`，網頁與測試共用同一份。
28 個讀法案例每筆標明課本頁碼；端到端 33 項涵蓋兩輪審查抓到的每個問題。

## 目前進度

本機完成，測試全過。**尚未部署、未實機驗收。**

## 未完成／待確認

- 🔴 **實機驗收是發布前的阻斷項**：教師觸控大屏與學生 iPad 的實際手感
  （拖曳靈敏度、字級、48px 是否真的夠大）
- 🔴 **無協助學習與實體遷移驗收尚未設計** —— 網頁過關不代表學會
- 🟡 建 GitHub repo、開 Pages
- 🟡 回填 `../knowledge-map/data/units.json` 的 U1 `tools` 欄位
- 🟡 `compare.html`／`calc.html` 沒有表徵重呈機制（只有 compose 有）
- 🟡 真鷹架未實作。Codex 建議：**只值得做一個最小試辦**
  （限定 compose 的跳位補 0，且要與現況比較無協助題表現，沒增益就停止）
- 🟡 U3 矩陣只涵蓋活動一二的一部分，4 個活動/目標尚未拆解

## 下一步

1. **實機驗收 U1**（這是阻斷項，做完才談部署）
2. 依 `../docs/互動教具設計流程.md` 走 U3。**注意 U3 會在四個地方卡住**：
   目標覆蓋不全、13 題考古題全未人工確認且 9 題資料不完整、
   媒介比較基準應是實體量角器而非學習單、缺無協助與實體遷移驗收

## 注意事項

- 改核心邏輯後務必 `python run-tests.py`
- **不要把邏輯搬回 HTML 內嵌**，那會讓測試變成測複本
- **不要把 `showScaffold()` 叫成鷹架**（見 CLAUDE.md「不能動的地方」）
- 中文讀法、位名一律以課本實際出現的為準，不要憑語感
- 修 bug 前先確認：這個修法會不會用超出定義域的值呼叫別的函式？
  會不會只治症狀不治算法？平行的另一個檔案要不要一起改？
