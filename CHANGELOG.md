# CHANGELOG — 互動定位板

## @2026-08-23 — v1.2 Codex 二輪審查修正 + 可重跑測試

第二輪審查判定**未通過**（9 高／6 中／1 低）。其中數條是我第一輪修正時自己製造的：
`setRange(7)` 當機、分組算理只移除觸發題目沒修算法、文件只改了核心章節。
16 條全數查證屬實，採納 15、部分採納 1（流程重構延後）。

### 我自己製造的迴歸（最該記取）
- **`setRange(7)`**：修「靜默倒退」時加了自動展開，卻沒檢查介面只有 6/8/9 三個 preset，
  造成 `Cannot set properties of null`、`range` 與畫面欄數不一致。
  修法：新增 `fitRange()` 映射到合法值，`setRange()` 加防呆，展開時明示範圍變更
- **分組算理**：上一輪只把會觸發錯誤的題目換掉讓測試變綠，**算法從頭到尾沒修**。
  修法：新增 `groupCalc()`，低組正規化為 0~9999、進位借位反映到高組，
  並把進位題加回來讓功能真的被練到
- **文件殘留**：改了核心章節卻漏掉流程文件另外兩處與 spec 文件兩處。
  修法：改完一律全專案掃描關鍵字驗收

### 其他修正
- 700ms 轉場 timer 改用 **generation token**，切關/切分頁使在途回呼失效
- `units.json` 38 筆迷思概念回填結構化 `evidence_level` 與 `scope`；
  閘門只讀該欄位（字串比對會把「學習目標明文列」誤判成困難明文），且**未通過即停止輸出**
- 考古題加驗答案 schema（題庫確實有 `answer=[1,2]` 的複選題）
- 觸控目標補到 48px；`compare.html` 相等數比較流程

### 術語誠實化
- `showScaffold()` 對照 van de Pol 三特徵（contingency／fading／責任移轉）**全未達成**，
  介面與程式一律改稱「**表徵重呈**」
- 「變異理論」改稱「**候選**關鍵特徵」——沒有學生作答證據前只是假設
- **更正我編造的課本敘述**：原寫⑨→⑩→⑪「一次抽掉一根鷹架」，
  回查課本 p5 發現⑩同時改變順序與位數，並非受控變異

### 可重跑測試（本次重點）
```bash
python run-tests.py
```
- `place-value-core.js` —— 核心邏輯抽出，**網頁與測試共用同一份**
- `place-value-core.test.js` —— 7 組、28 個讀法案例，每筆標明課本頁碼
- `e2e.spec.py` —— 五頁端到端 33 項
- `run-tests.py` —— 單一入口

以前用 `node -e "...eval..."` 把 `<script>` 挖出來跑，測的是複本、換行一改就壞，
等於沒有可重跑的測試。現在網頁 `<script src>` 載入、測試 `require` 同一個檔案。

## @2026-08-23 — v1.1 Codex 交叉審查修正（22 條全數採納）

送 Codex 做模式 B 交叉審查，回饋 22 條，逐條查證後**全數採納、零駁回**。
可驗證的指控幾乎全部屬實，包含三項會直接教錯的內容錯誤。

### 教學內容修正
- **具體物層級**：補回 1000 元鈔票。課本 p2 的起點是「10 張 1000 元紮成一疊 ＝ 10000 元」，
  原本直接從 1 萬元開始，跳掉第一層化聚。「紮成一疊」現在只在千→萬那次講
- **compare.html 顏色**：位數不同時改整列標示。原本逐位標色會把短數字的高位數字標成「勝」，
  與「位數多的比較大」互相矛盾，且顏色那邊是錯的
- **兩題挑戰解析**：114-5 右組是 0700 不是 700；109-5 概數題須先各自取概數再相減
- **calc.html 練習題**：移除自組的 123992+66889（右組會溢位成五位數），改用課本數字
- **提款單改寫題**：欄名「仟」「拾」各出現兩次，補上「萬左邊／萬右邊」消歧義

### 程式缺陷修正
- `99999999+1` 不再靜默倒退成 90000000；自動展開位數，到真上限則保留原值並提示
- 九位數分組修正為 `1 2345 6789`；`readNumber` 加上明確範圍契約
- 拖曳真的認落點，放錯位拒絕並提示正確位名；進位動畫改用 overlay 避免被重建掉
- 關卡推進綁定答對狀態，空白連按無法過關；轉場計時器防重複
- 12300 題補齊 5 個輸入格；簡記法擋負數；五頁觸控目標統一 ≥48px

### 驗證
讀法回歸測試 28 例通過；四批修正各自的 Playwright 驗證全過；
五頁 JS 語法檢查通過；完整回歸未破壞既有功能。

## @2026-08-22 — v1.0 四頁完成

四上第一單元「一億以內的數」互動教具，五個頁面（導覽＋四個活動頁）。
設計文件：`../docs/superpowers/specs/2026-08-22-place-value-board-design.md`

### board.html — 定位板：記數與讀數（活動一＋二）
- 探索模式：拖曳或點擊鈔票（1萬／10萬／100萬／1000萬）放入定位板
- **滿 10 自動進位**，對應課本「10 張紮成一疊」的操作
- 即時顯示阿拉伯數字、中文讀法、位數
- **零的規則視覺化**：依規則讀出來的零標紅色，並提示「有 N 個 0 依規則不讀出來」
- 可切換 6／8／9 格（十萬以內／一億以內／含億位）與「四位一組」分隔線
- 練習模式三關：記數、讀數、寫數。題目刻意集中於含 0 的數

### compose.html — 分解與合成（活動一⑦–⑪）
- 探索模式：把數拆成積木，點一下可拿掉，即時看數字如何變化
- 練習五關：分解／位值辨識／合成／合成打亂／**合成跳位**
- 跳位關答錯時明確提示「沒被提到的位要放 0」

### compare.html — 數的大小比較（活動三）
- 探索：兩數排入上下兩排定位板，逐位點亮，標出哪一位分出勝負
- 位數不同時直接套用課本的「方法二：用位數比」
- 練習：填 ＞＜＝，答錯時指出是哪一位分出勝負

### calc.html — 大數的加減（活動四＋素養吧）
- 探索：逐位直式計算，進位／借位標記
- 可切換課本教的「377萬＋92萬」簡記法
- 練習採**兩步驟拆解**（先算個那組、再算萬那組、最後合併），
  對應課本素養吧「分別用幾個一、幾個萬來計算」

### 驗收
- **中文讀法回歸測試 28 例全通過**，涵蓋課本每一個標示過讀法的數字
  （29100、30100、38005、48014008、47980009、46500000、48004000、23264640、
  8010007、60220000、41000900、23400000、100000000、80100006 等）
- Playwright 逐頁測試 27 項全通過，零主控台錯誤：
  index 連結、board 進位與讀法與三關卡、compose 分解與跳位提示、
  compare 兩種比較策略、calc 逐位計算與兩步驟練習

### 修掉的兩個 bug（詳見 PITFALLS.md）
1. `readNumber(100000)` 產出「一十萬」，課本 p2 明寫「十萬」。
   規則應為「一十」只在整個數開頭簡化，非開頭保留（課本 p13「八千零一十萬零六」）
2. `compare.html` 分出勝負後繼續點擊，會把正確結論覆蓋成「每一位都一樣」的錯誤結論
