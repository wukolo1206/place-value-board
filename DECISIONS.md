# DECISIONS.md — 互動定位板

## 為什麼先做 U1 定位板而非 U3 量角器
`../docs/互動網頁引導模式分析.md` 的評估把 U3 虛擬量角器列為第一梯隊、U1 定位板列第二。
使用者選擇先做 U1，理由是按單元順序教學，第一單元最快用到。

## 為什麼四頁一次做完
原提案建議先做 `board.html` 試手感。使用者選擇四頁一次完成。

## 為什麼讀法函式回傳 token 陣列而非字串
因為 UI 需要把「依規則讀出來的零」標成紅色、「被略過的零」不顯示。
若回傳字串就無法區分哪個「零」來自哪條規則。

## 為什麼題目寫死在頁面裡
沿用 `../09_統計表-game/table1d.html` 的做法。題目數量小且穩定（全部取自課本），
外接 JSON 會讓檔案變成兩個、部署時多一層失敗可能，不划算。

## 為什麼拖曳與點擊都支援
教師用觸控大屏時常站在教室中後方操作，點擊比拖曳可靠；
學生用 iPad 近距離操作，拖曳較符合「把鈔票放進去」的具體經驗。
